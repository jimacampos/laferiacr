# Backlog — La Feria CR

**Status:** 🟢 Living document · _Last updated: 2026-07-07 (analytics + feedback)_

The single place to capture future features and decisions that aren't in the active phase. Add items
with the **`backlog-add`** skill (it appends a row using the format below). Promote items into a phase
in the [roadmap](roadmap.md) when they're ready to schedule.

## How to use
- **IDs:** features use `BL-NNN`; open questions/decisions use `OQ-NNN`. Never reuse an ID.
- **Type:** `feature` · `enhancement` · `bug` · `tech-debt` · `infra` · `research` · `decision`.
- **Priority:** `P1` (next up) · `P2` (soon) · `P3` (someday).
- **Status:** `idea` · `accepted` · `scheduled` (note phase) · `done` · `dropped`.
- Keep one line per item; link related docs/ADRs.

## Feature backlog
| ID | Title | Type | Priority | Status | Area / Phase | Notes & links |
| --- | --- | --- | :--: | --- | --- | --- |
| BL-001 | Photos of markets & stalls (north star) | feature | P1 | scheduled | Phase 8 | Blob+CDN + AI Content Safety; [roadmap](roadmap.md) |
| BL-002 | Reviews & ratings | feature | P2 | idea | Phase 8 backlog | After photos |
| BL-003 | Products & seasonal prices | feature | P2 | idea | Phase 8 backlog | Per-market product lists |
| BL-004 | Favorites & reminders | enhancement | P2 | idea | Phase 8 backlog | Needs accounts ([prd](prd.md)) |
| BL-005 | Vendor/organizer official accounts (Market Steward role) | feature | P3 | idea | Phase 8 backlog | Verified claim; [rbac](../architecture/rbac.md) |
| BL-006 | Reputation-weighted confirmations | enhancement | P2 | scheduled | Phase 6 | [moderation-trust](../architecture/moderation-trust.md) |
| BL-007 | Anti-sybil / anomaly heuristics | enhancement | P2 | scheduled | Phase 6 | Abuse resistance |
| BL-008 | PWA install + offline market list | feature | P2 | scheduled | Phase 7 | [roadmap](roadmap.md) |
| BL-009 | Opt-in push notifications ("open near you") | feature | P3 | scheduled | Phase 7 | Azure Notification Hubs |
| BL-010 | "Senior mode" (extra-large, simplified UI) | enhancement | P3 | idea | Accessibility | vs scalable settings; [accessibility](../accessibility.md) |
| BL-011 | Front Door + CDN caching for read-heavy browsing | infra | P3 | idea | Infra | Add when traffic warrants ([infrastructure](../architecture/infrastructure.md)) |
| BL-012 | Speed up CD deploy pipeline (~12 min/run) | tech-debt | P3 | done | Infra / CI-CD | **Done (2026-07-07) — measured 6 min 26 s (was ~12 min, ~46% faster).** In [cd.yml](../../.github/workflows/cd.yml): (A) removed the redundant double bicep deploy — the full-infra "provision" step now only bootstraps on the **first** deploy for an environment (when no ACR exists); later runs go straight to build → a single authoritative deploy; (B) added `actions/setup-node` + npm cache so the migrate/seed `npm ci` is cached (matches ci.yml); (C) gate migrate/seed to run **only when `prisma/**` or `src/data/ferias.json` changed** (manual runs always run them; safe fallback when no base commit). Remaining idea if more speed is needed: revisit `az acr build` layer reuse / Dockerfile. |
| BL-013 | Add Google as an Entra External ID sign-in provider | enhancement | P3 | idea | Auth / Phase 2 follow-up | Nice-to-have one-tap login alongside working email OTP; portal-only (Google Cloud OAuth client → Entra IdP → add to user flow), no app code. [runbook](../../deploy/entra-external-id-setup.md), [ADR-0005](../decisions/0005-identity-entra-external-id.md); see [OQ-005](#open-questions--decisions-to-make) |
| BL-014 | "Needs verification" discovery view for signed-in users | enhancement | P2 | idea | Phase 3 follow-up | Dev testers can't easily find which ferias need validation — no way to discover markets with pending proposals or missing hours/location. Add a signed-in queue/filter surfacing markets that need confirmation (pending proposals awaiting votes, and/or null `hours_text`/`location`), ordered by need. Reuses `getMarketContributions`/`proposals` state; entry point from Header or home filter. [moderation-trust](../architecture/moderation-trust.md), [prd](prd.md), [phase-3-tasks](phase-3-tasks.md) |
| BL-015 | Non-map alternative for location editing (a11y) | enhancement | P2 | idea | Accessibility / Phase 3 follow-up | Location suggestion currently requires pin-drop or "use my location" — no keyboard/screen-reader-friendly path (e.g. address or manual lat/lng entry). Surfaced by docs-check on Phase 3. Add a text/address input as an equivalent to the map interaction in `SuggestLocation`. [accessibility](../accessibility.md), [phase-3-tasks](phase-3-tasks.md) |
| BL-016 | Enable CAPTCHA on anonymous contribution writes | enhancement | P2 | accepted | Anti-abuse / Phase 3 follow-up | Provider-agnostic CAPTCHA seam is already built and flag-gated **off** (rate limiting is live). To activate: pick a provider (hCaptcha / Cloudflare Turnstile), add keys to the env/Key Vault, flip the flag on dev, then verify before broad public launch. [ADR-0012](../decisions/0012-anti-abuse-rate-limiting-and-captcha.md), [security-privacy](../architecture/security-privacy.md) |
| BL-017 | Structured per-day hours + editable market days | feature | P2 | idea | Data model / Phase 3 follow-up | Tester feedback: a feria may operate on days not in our seeded `days`, and different days can have different schedules. Today `days` is a fixed seed list and `hours_text` is one free-text string for the whole market. Move toward structured, per-day hours (e.g. `{ fri: "6am–12pm", sat: "6am–2pm" }`) and let the community correct/add days via the contribution loop. Larger data-model change (may graduate to an ADR); structured hours were deferred in Phase 3. [data-model](../architecture/data-model.md), [prd](prd.md), [phase-3-tasks](phase-3-tasks.md) |
| BL-018 | Make the moderation Audit log human-readable | enhancement | P2 | idea | Moderation / Phase 4 follow-up | Audit log shows truncated raw UUIDs for ACTOR/TARGET (e.g. `33a98ef2…`, `user76215dd3…`, `market1c5eeb54…`) so entries aren't usable. Resolve to labels: user = display name (fallback email), market = name + slug; show the full UUID on hover (title attr). Label a null actor as **System/Automated**; empty REASON (e.g. `grant_role`) stays `—`. Joins `moderation_actions` → users/markets. **Scoped approach (2026-07-03, not yet built):** add a pure `src/lib/contributions/auditLabels.ts` (+ colocated `auditLabels.test.ts`) mapping ids→labels (fallback user displayName→email→short id; market `name (slug)`); enrich `listModerationActions` in `moderation.ts` with batch joins (users + markets + proposals→parent market) and render labels in `AuditTable.tsx` with the full UUID in `title`, null actor → `t("audit.actor.system")`. **Confirmed decisions:** (1) fully resolve proposal targets → parent market name+slug + field (e.g. "Feria de Zapote · hours") and config targets → the setting key from `metadata.key` (e.g. `confirmation_threshold`); (2) also humanize the ACTION column into bilingual `audit.action.*` labels (ES/EN), unknown codes fall back to the raw code. Target types in play: user (grant/revoke/ban/lift), market (hide/unhide/override/revert), proposal (remove/resolve/dismiss report), config (set_config, no targetId). [rbac](../architecture/rbac.md), [moderation-trust](../architecture/moderation-trust.md) |
| BL-019 | Explain each role on the moderation Roles screen | enhancement | P3 | done | Moderation / Phase 4 follow-up | **Shipped (2026-07-07):** added a `RoleLegend` explainer atop the Roles screen — an accessible info **(i)** disclosure per role (Member/Trusted/Community Safety/Super Admin) that reveals what each grants. Click / Enter / Space toggles, Escape closes, `aria-expanded`/`aria-controls`; keyboard + touch friendly (not hover-only). Bilingual copy in `dictionaries.ts` (`role.*.desc`, `roles.legendTitle`) sourced from the [rbac](../architecture/rbac.md) capability matrix. Pure `roleInfo.ts` legend model (+ test asserting every role has a non-empty ES/EN label + description). [accessibility](../accessibility.md) |
| BL-020 | Moderation panel: "markets requiring attention" queue | enhancement | P2 | done | Moderation / Phase 4 follow-up | **Shipped (2026-07-03)** as a read-only **confirmation-backlog** view at `/admin/attention` (Community Safety+ via the `/admin` layout gate). Lists markets with `pending` suggested changes that still need more net confirmations to reach threshold N, **ordered oldest/most-stale first**; each row shows the field, progress (`net X of N`, how many more needed) and waiting-since, and links to the market page to confirm. **Scope change from the original idea:** dropped the "missing data (null hours/location)" signal (nearly every market starts empty, so it was noise) and kept **open reports in their own Reports tab** — this view focuses purely on the confirmation backlog. Pure ranking in `attentionQueue.ts` (+ tests); `getConfirmationBacklog()` data fn; `AttentionQueue` component; new nav tab. Distinct audience from [BL-014](#feature-backlog) (signed-in end-user discovery) but shares the underlying pending-proposal signal; relates [OQ-009](#open-questions--decisions-to-make). [moderation-trust](../architecture/moderation-trust.md) |
| BL-021 | Restyle/relocate inline market-page moderation controls | enhancement | P3 | idea | UI / Phase 4 follow-up | On public `/market/[slug]`, a signed-in moderator sees moderation tools at the top — functional but intrusive/ugly. Restyle/relocate to a less dominant panel or compact bar; leave the rest of the page as-is. Verify both states: moderator (tidy) and anonymous (unaffected, no leakage). [prd](prd.md), [accessibility](../accessibility.md) |
| BL-022 | Investigate: user can't confirm location for Zapote market | bug | P2 | in progress | Contributions / Phase 3–4 | **Investigated (2026-07-03): not a functional bug.** Dev data — Zapote location proposal is legitimately `pending` at **net 1 of N=2** (one confirm from a real distinct user; submitted anonymously; valid coords), so the market's `location` stays `NULL` until a **second, different** account confirms. Confirm/promote path (incl. PostGIS `ST_MakePoint(lng,lat)`) is correct. Root cause is **UX/messaging**, not logic. **Fix shipped (A+B):** (A) surface specific bilingual vote errors via pure `voteErrors.ts` `voteErrorKey()` (+ tests) in `ProposalList` instead of one generic message (covers `auth_required`/`banned`/`not_found`/`not_open`/`self_vote`/network); (B) when the viewer has already confirmed but it's still pending, show *"You've confirmed — waiting on N more people"* instead of a bare "N more needed", so a single confirmer understands it needs another distinct person. **Still open:** get the reporter's exact error + steps to rule out a real transport/500 case. [moderation-trust](../architecture/moderation-trust.md), [phase-3-tasks](phase-3-tasks.md) |
| BL-023 | Home page: name-first search hero | enhancement | P1 | done | UI / Phase 4.5 | Redesign the home page around a prominent **name-first search** ("Encontrá tu feria") with a short bilingual value prop + light market count; search filters by name (accent-insensitive via `filters.ts`) as the primary find path. **Shipped** (name-first hero + market count). [roadmap](roadmap.md), [prd](prd.md) (FR-60, FR-65) |
| BL-024 | Redesign MarketCard: name-first (days + location when present) | enhancement | P1 | done | UI / Phase 4.5 | Card **leads with the name**; shows **days open**; shows a 📍 **location indicator only when the market has coordinates** (link to the map); **drop region + phone** from the card (phone stays on the market detail page). Lightweight present-only indicator, no 66 mini-maps ([NFR-30](prd.md)). **Shipped** (`hasLocation` plumbed to the home list; 📍 deep-links to `/market/{slug}#location`). [prd](prd.md) (FR-61, FR-64) |
| BL-025 | Drop the "this weekend" default; day filter optional | enhancement | P1 | done | UI / Phase 4.5 | Home no longer defaults to a time-based view (scheduled `days` ≠ confirmed open/closed); day chips become an optional secondary filter. **Shipped** (`DEFAULT_FILTERS.day = "all"`). [prd](prd.md) (FR-62) |
| BL-026 | Demote/remove the region filter on the home page | enhancement | P2 | done | UI / Phase 4.5 | Region is administrative, not how users find markets (a feria's **name** already implies its place). **Decided ([OQ-013](#open-questions--decisions-to-make)): removed from the primary UI** — the region `<select>` is gone; `regionId` filter logic is retained in `filters.ts` but unused by the home UI. [prd](prd.md) (FR-63) |
| BL-027 | "Near me" nearest-markets ranking (deferred) | feature | P2 | idea | UI / Phase 4.5 (parked) | Distance-sorted "near me" home once coverage grows; needs coordinates for most markets (**2/66 today**). Depends on the geocoding decision [OQ-014](#open-questions--decisions-to-make) / BL-028; explicit geolocation consent (NFR-32). [prd](prd.md) |
| BL-028 | Geocode official markets → approximate coordinate baseline | infra | P2 | idea | Data / Phase 4.5 enabler (parked) | Geocode the 66 official markets (name + region + "Costa Rica") via Azure Maps to an **approximate official** location, **only where `location` is null**; never overwrite community-verified pins; record provenance/approximate. Unblocks BL-027. Decision [OQ-014](#open-questions--decisions-to-make). [data-model](../architecture/data-model.md) |
| BL-029 | Home-redesign polish pass (brand + paginated A–Z + collapsible day filter + live search) | enhancement | P2 | done | UI / Phase 4.5 | Follow-up to BL-023/024 to make the home feel intentionally designed (not "basic") within our data reality (no photos/coords/hours). **Shipped:** shared hand-built **SVG brand mark + wordmark** (`BrandMark.tsx`, reused in Header + Hero) + a decorative hero motif; **pagination** — the directory paginates **10 markets per page** (`Pagination.tsx`; pure `paginate`/`pageCount`/`sortFeriasByName` in `home.ts` + tests); **A–Z scannability** — alphabetical sections with a **jump index moved to the bottom** (`AlphaIndex.tsx`; present letters jump to the page + section via `letterFirstPage`, absent letters disabled); **collapsible day filter** — `FilterBar.tsx` day chips are hidden behind a disclosure toggle (`aria-expanded`, auto-open when a day is set), reinforcing that day filtering is secondary (BL-025); **live search feedback** — accent-insensitive **match highlighting** (pure `search.ts` `highlightSegments` + tests; `<mark>` in cards), a **clear (✕)** button, **`/`-to-focus** shortcut, live result count, and a **query-aware empty state**; wider `max-w-5xl` responsive grid (2–3 col). The hero market/region **count was removed** per feedback. Region stays off cards (OQ-013). [prd](prd.md) (FR-65, FR-67), [accessibility](../accessibility.md) |
| BL-030 | Pre-launch: analytics (GA4 + App Insights), feedback channel, help page | feature | P1 | done | Launch readiness | **Shipped & deployed to `dev`** (PR #15) ahead of the reduced (IG/FB) soft launch. **Analytics:** GA4 via `@next/third-parties` in `layout.tsx`, gated on `NEXT_PUBLIC_GA_ID` (build-time build-arg via Dockerfile + CD, sourced from the `dev` env variable `G-4TG80GPEVP`; **verified live** in the served HTML) — page views + referral source to distinguish IG vs FB; UTM-tag shared links; server request/latency/error telemetry via `src/instrumentation.ts` (Azure Monitor OpenTelemetry distro, gated on the already-injected `APPLICATIONINSIGHTS_CONNECTION_STRING`). **Feedback:** sign-in-only channel — `Feedback` model + migration, `POST /api/feedback` (401 when signed out, per-user rate limit), bilingual `/feedback` form (footer link) that prompts sign-in, and a moderator-gated `/admin/feedback` view + dashboard count. **Help:** bilingual `/help` (Ayuda) page sourced from [contributing.md](../community/contributing.md), linked from header + footer. [infrastructure](../architecture/infrastructure.md) |
| BL-031 | Cookie-consent banner for analytics (deferred) | enhancement | P3 | idea | Privacy / Launch follow-up | GA4 sets analytics cookies. A short bilingual privacy note ships in the footer (BL-030), but there is no consent banner/opt-out yet — acceptable for a small soft launch, revisit before broad/EU-facing launch. Consider Consent Mode v2 or a lightweight banner gating `NEXT_PUBLIC_GA_ID`. [security-privacy](../architecture/security-privacy.md) |

## Open questions / decisions to make
| ID | Question | Priority | Status | Related |
| --- | --- | :--: | --- | --- |
| OQ-001 | Confirmation threshold **N** starting value | P1 | ✅ resolved (Phase 3: **N = 2**, net confirmations; configurable via `CONFIRMATION_THRESHOLD`) | [moderation-trust](../architecture/moderation-trust.md), [ADR-0008](../decisions/0008-promotion-automated-confirmation-and-roles.md) |
| OQ-002 | When to enable reputation weighting of votes | P2 | open (deferred to Phase 6; Phase 4 `trusted` is a manual marker, no weighting) | [moderation-trust](../architecture/moderation-trust.md) |
| OQ-003 | Moderator vetting & regional scoping | P2 | open (Phase 4 decision: **manual grant only, scoping deferred** — `user_roles.scope` kept but not enforced; grants global) | [rbac](../architecture/rbac.md), [ADR-0014](../decisions/0014-rbac-moderation-queue-and-temp-bans.md) |
| OQ-004 | Duplicate-detection strictness for new markets | P2 | open | [ADR-0009](../decisions/0009-community-submitted-markets.md) |
| OQ-005 | Entra sign-in providers beyond Google | P2 | open | [ADR-0005](../decisions/0005-identity-entra-external-id.md) |
| OQ-006 | Pin prod ACA to min-1 replica vs scale-to-zero | P2 | open | [infrastructure](../architecture/infrastructure.md) |
| OQ-007 | Private networking (VNet/Private Link) for DB | P3 | open | [infrastructure](../architecture/infrastructure.md) |
| OQ-008 | Show contributor display names publicly vs pseudonymous | P2 | open (Phase 3 default: **pseudonymous** — no public names) | [security-privacy](../architecture/security-privacy.md) |
| OQ-009 | Auto-quarantine threshold (reports before auto-hide) | P2 | open (Phase 4 decision: **no auto-quarantine** — the queue sorts by open-report count and moderators act manually, to avoid false-report brigading; revisit later) | [moderation-trust](../architecture/moderation-trust.md), [ADR-0014](../decisions/0014-rbac-moderation-queue-and-temp-bans.md) |
| OQ-010 | Appeal channel & SLA | P3 | open (needed once temp-bans/removals are live in Phase 4; not built yet) | [content-guidelines](../community/content-guidelines.md), [moderation-trust](../architecture/moderation-trust.md) |
| OQ-011 | CI accessibility gate (tool + threshold) | P3 | open | [accessibility](../accessibility.md) |
| OQ-012 | Retention windows for IP/device abuse signals | P3 | open | [security-privacy](../architecture/security-privacy.md) |
| OQ-013 | Home page region filter: remove entirely vs keep as an optional secondary filter | P2 | **decided (Phase 4.5): removed** from the primary UI (BL-026) — the name already implies the place; `regionId` filter logic kept in `filters.ts` but unused by the home UI | [prd](prd.md), [roadmap](roadmap.md) |
| OQ-014 | Whether/how to geocode official markets for "near me" (approximate official baseline vs community-only) | P2 | open (Phase 4.5) | [data-model](../architecture/data-model.md) |

---
_Add new items at the bottom of the relevant table via the **`backlog-add`** skill._
